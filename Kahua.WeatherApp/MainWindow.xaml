<?xml version="1.0" encoding="utf-8"?>

<Window
    x:Class="Kahua.WeatherApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kahua.WeatherApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d">

    <Grid RequestedTheme="Light" x:Name="Grid">
        <Grid.Resources>

        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*" />
            <RowDefinition Height="4*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="LightBlue" />
        <Border Grid.Row="0" Grid.Column="1" Background="LightSkyBlue" />
        <Border Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="0" Background="DeepSkyBlue" />
        <StackPanel Width="300" VerticalAlignment="Center">
            <StackPanel.Resources>
                <Thickness x:Key="Margin">0,10,0,0</Thickness>
                <Style x:Key="LabeledTextBoxStyle" TargetType="local:LabeledTextBox">
                    <Setter Property="Margin" Value="{StaticResource Margin}" />
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="Width" Value="300" />
                </Style>
                <Style x:Key="ButtonStyle" TargetType="Button">
                    <Setter Property="HorizontalAlignment" Value="Right" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Margin" Value="{StaticResource Margin}" />
                </Style>
            </StackPanel.Resources>
            <local:LabeledTextBox Label="Long:" Style="{StaticResource LabeledTextBoxStyle}" />
            <local:LabeledTextBox Label="Lat: " Style="{StaticResource LabeledTextBoxStyle}" />
      
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
               
                <Button Style="{StaticResource ButtonStyle}" Click="ButtonBase_OnClick">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE707;" />
                    <ToolTipService.ToolTip>
                        <ToolTip Content="Use current location" />
                    </ToolTipService.ToolTip>
                </Button>
                <Button Style="{StaticResource ButtonStyle}" Click="ButtonBase_OnClick">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE721;" />
                </Button>
            </StackPanel>
            <ProgressBar HorizontalAlignment="Stretch" IsIndeterminate="True" Visibility="Visible" Opacity="0" />
        </StackPanel>

        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <local:InfoDisplay></local:InfoDisplay>
        </StackPanel>
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="0"
               HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <local:InfoDisplay Grid.Column="0" Margin="20"></local:InfoDisplay>
            <local:InfoDisplay Grid.Column="1" Margin="20"></local:InfoDisplay>
            <local:InfoDisplay Grid.Column="2" Margin="20"></local:InfoDisplay>
            <local:InfoDisplay Grid.Column="3" Margin="20"></local:InfoDisplay>

        </Grid>
    </Grid>
</Window>